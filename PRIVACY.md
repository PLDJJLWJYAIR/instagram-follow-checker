# 隐私说明

本扩展无独立服务器、分析追踪或第三方数据上传代码，不读取或保存密码，不要求提交登录凭证。它在使用者已登录的 Instagram 页面上执行列表和搜索操作；这些操作会与 Instagram 通信。

## 本地数据

使用 `chrome.storage.local` 保存读取到的账号列表、扫描结果、历史快照、断点与导出状态。数据属于当前 Chrome 用户配置，不使用 `chrome.storage.sync`。本项目不内置跨设备同步。

TXT 导出包含使用者账号及名单，应按个人资料处理。不要将真实报告、Cookie、登录凭证或个人名单附在公开 Issues、提交记录或 Release 中。

## 权限用途

| 权限 | 用途 |
| --- | --- |
| storage | 保存本地结果、断点和状态 |
| activeTab / tabs | 识别当前 Instagram 标签页及脚本版本 |
| scripting | 页面缺少扩展脚本时加载脚本 |
| downloads | 打开本地 TXT 保存对话框 |
| instagram.com 站点权限 | 在授权站点运行扫描脚本 |

发布包只包含程序和说明，不包含浏览器用户配置、历史账号名单、设备名称或开发者本机路径。`build_release.py` 使用明确白名单，排除运行数据和隐藏文件。

“清除结果”删除当前结果和断点；已导出到磁盘的 TXT 需要使用者自行管理。旧版本可能存在不再使用的关注库缓存；彻底删除全部扩展存储可卸载扩展（会失去历史快照），再重新安装。
