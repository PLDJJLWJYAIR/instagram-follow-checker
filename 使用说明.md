# 安装与使用说明

## 使用前准备

- 使用 Windows、macOS 或 Linux 的桌面 Google Chrome 当前稳定版。
- Instagram 网页语言选择简体中文或英文。
- 自行登录 Instagram；扩展不会索要账号密码。
- 公司或学校管理的设备可能禁止开发者扩展；手机 Chrome 不适用。

## 安装

1. 在仓库 Releases 下载 `instagram-follow-checker-5.2.0.zip`。
2. 解压到一个长期保留的文件夹：Windows 右键“全部解压”，macOS 双击，Linux 使用归档管理器。
3. Chrome 地址栏输入 `chrome://extensions`。
4. 打开“开发者模式”，点击“加载已解压的扩展程序”。
5. 选择**直接包含 `manifest.json` 的文件夹**。不要选择 ZIP，不要双击 `popup.html`。
6. 在 Chrome 扩展菜单固定本扩展。安装后不要移动或删除源文件夹。

## 必须先进入自己的个人主页

1. 在 Instagram 点击自己的头像，进入自己的个人主页。
2. 页面上应能看到你的用户名、粉丝数和关注数。
3. **不要停在首页信息流、帖子页、私信页或其他人的主页。**
4. 安装或更新扩展后，刷新 Instagram 主页。
5. 点击扩展图标 → **一键检查未互关**。

扫描期间保留 Instagram 标签页，不要刷新、切换账号、导航离开或手动操作列表。扩展面板关闭后，页面脚本仍可继续工作。再次打开扩展查看进度和结果。

## 自动扫描规则

- 读取当前关注与粉丝，两张列表都会更新。
- 每张列表最多两轮：首次扫描加一次补扫，等待 5 秒后补扫。
- 实际数量少于页面显示 1–3 个时不再反复追数，进入候选核验。
- 程序保存真实数量与缺口；容差不等于已获得全部名单。
- 超时、没有搜索框等情况列为“待确认”，不会直接当作未互关。
- 若仍是部分完成，可稍后再点击继续，程序会保存断点。

## 理解结果

“当前未互关”包括新关注但没有回关的人，第一次运行也可检查。

“从粉丝中消失”需要两次可比较的历史快照；改名、停用也可能造成变化，不能据此单独断定主动取关。

“部分完成”表示未覆盖全部账号或仍有待核验候选。Instagram 页面结构和限流可能阻止全量扫描。扩展不自动取消关注任何人。

## 本地另存为 TXT

点击 **本地另存为 TXT**，在 Chrome 保存窗口选择位置。导出最近一次已保存的结果；新扫描还没结束时可能导出上一轮结果。

文件包含：扫描时间、导出时间、本地时区、UTC、毫秒、读取数量、页面数量、缺口、已核验未互关、待确认、历史变化以及当次读取的原始名单。

没有已保存结果时，按钮会说明原因。只有断点时不会把未核验数据当作最终名单导出。TXT 含个人名单，分享前请检查。

## 更新

1. 保存自己需要保留的结果。
2. 将新版程序文件放进原扩展文件夹。
3. 在 `chrome://extensions` 点击重新加载。
4. 刷新 Instagram 个人主页。

通常无需卸载；卸载可能删除本地扩展数据。“清除结果”会删除保存结果和断点，不会删除已导出的 TXT。

## 常见问题

| 现象 | 处理方法 |
| --- | --- |
| 找不到关注或粉丝入口 | 进入自己的主页，检查页面语言，刷新页面 |
| 扩展面板自动消失 | Chrome 面板失焦可能关闭；保留 Instagram 标签页，稍后重新打开 |
| 很久没有新增账号 | 稍后再试，避免连续多次触发；断点无法绕过平台限制 |
| 下载无反应 | 查看按钮下方状态，重新加载扩展使下载权限生效 |
| 文件在哪里 | 查看 Chrome 下载列表；保存位置由你选择 |
| 换电脑找不到结果 | 结果保存在原 Chrome 用户配置，本项目不自动跨设备同步 |

## 分发边界

这是解压加载的分享包，并非 Chrome 网上应用店审核产品。公众通过“添加至 Chrome”安装需要另行上架审核。安装与分发规则可参考 [Chrome 官方教程](https://developer.chrome.com/docs/extensions/get-started/tutorial/hello-world)及 [分发说明](https://developer.chrome.com/docs/extensions/how-to/distribute)。

跨平台代码及测试不等于所有系统和 Instagram 账号界面均经过实际验证。
