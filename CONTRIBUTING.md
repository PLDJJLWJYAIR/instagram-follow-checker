# Contributing

感谢改进这个项目。提交问题时请提供扩展版本、Chrome 版本、系统、页面语言、操作步骤和预期结果。截图中遮盖用户名、头像与个人名单；不要提交 Cookie、凭证、真实导出文件或浏览器配置。

## 本地开发

1. 在 Chrome 中将仓库根目录加载为已解压扩展。
2. 修改后重新加载扩展并刷新自己的 Instagram 主页。
3. 用 `node --test scanner.test.cjs` 执行测试。
4. 用 `python3 build_release.py` 生成发布包及校验文件。

尽量让改动集中在一个问题。修复误判、重试或持久化问题时请补充有意义的回归测试；不要把外部网站的真实个人数据写入测试。

PR 中说明问题、改动后的行为、验证方法与尚未验证的部分。网页结构相关变更要说明测试的页面语言和场景，不能将模拟测试表述为已完成真实账号验证。

本仓库暂未指定开源许可证。提交贡献前请先与维护者确认相应授权范围。
